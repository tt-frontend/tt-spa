import { FC } from 'react';
import { Wrapper } from './TasksListPage.styled';
import { Props } from './TasksListPage.types';

export const TasksListPage: FC<Props> = ({}) => {
    return <Wrapper></Wrapper>;
};
